To run this project :

1.if you want to directly test live model just run live.py
2.if you want to run model.py first unzip mydata folder which might take some time 

All this need the required libraries to be installed on the host system
